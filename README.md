# flutter-glass

Liquid glass shader demo using Flutter 3.47 + Impeller.

- 基于 `LiquidGlass-opengl` 仓库的 SDF + 法线 + 折射原理
- 在 Flutter 下通过 `FragmentProgram` + Impeller Fragment Shader 实现
- 当前版本使用 UV 渐变作为背景，后续可以接真实场景纹理
